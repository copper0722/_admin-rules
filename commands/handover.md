Read `/Users/copper/Library/CloudStorage/Dropbox/Vault_Binary/ref/skills/handover/SKILL.md` and execute the skill. Arguments: $ARGUMENTS

If no arguments or just `/handover` → READ mode (show latest handover).
If arguments contain "write" → WRITE mode (record this session to DB).
